# NewLife.Office 需求文档

## 0. 实现状态概览（2026-03-15）

| 格式 / 模块 | 读 | 写 | 模板 | 转换 | 实现状态 |
|------|:---:|:---:|:---:|:---:|------|
| **Excel xlsx** | ✅ | ✅ | ✅ | — | 全面完成，含图表/透视表/条件格式/图片 |
| **Excel xls 读** | ✅ | — | — | — | BiffReader 已实现，OLE2/CFB 基础 |
| **Excel xls 写** | — | 🔄 | — | — | 规划中（M17-02） |
| **Word docx** | ✅ | ✅ | ✅ | HTML/PDF | 全面完成，含 WordHtmlConverter/WordPdfConverter |
| **Word doc 读** | ✅ | — | — | — | DocReader 已实现（纯文本+段落） |
| **PPT pptx** | ✅ | ✅ | ✅ | 🔄 | 全面完成，含图表/母版/切换动画 |
| **PPT ppt 读** | ✅ | — | — | — | PptReader 已实现（OLE2，提取幻灯片文本） |
| **PDF** | ✅ | ✅ | — | — | 全面完成，含 Fluent API/合并/拆分/加密 |
| **Markdown** | ✅ | ✅ | — | HTML | CommonMark+GFM 已实现，→Word/PDF 还未开始 |
| **RTF** | ✅ | ✅ | ✅ | — | 读写已实现（段落/表格/模板） |
| **ODS** | ✅ | ✅ | — | — | 读写已实现 |
| **OLE2/CFB** | ✅ | ✅ | — | — | CfbReader+CfbWriter 已实现 |
| **EML/MSG** | 🔄 | 🔄 | — | — | 规划中 |
| **iCalendar** | 🔄 | 🔄 | — | — | 规划中 |
| **vCard** | 🔄 | 🔄 | — | — | 规划中 |
| **EPUB** | 🔄 | 🔄 | — | — | 规划中 |
| **XPS** | 🔄 | 🔄 | — | — | 规划中 |

> ✅ 已实现 | 🔄 规划中 | — 暂不支持

---

## 1. 项目定位

NewLife.Office 是一个功能全面的 .NET 办公自动化库，覆盖 **Excel（xlsx/xls）、Word（docx/doc）、PPT（pptx/ppt）、PDF**、Markdown、RTF、ODS、Email（eml/msg）、iCalendar、vCard、EPUB、XPS 等主流文档格式，以及基于 OLE2/BIFF8 的老式二进制格式（.xls/.doc/.ppt）。  
**核心定位**：以 MIT 开源协议为社区提供主流办公文档的**全面操作能力**，覆盖创建、读取、编辑、模板填充、合并拆分等常见业务场景，让开发者无需依赖商业库即可完成绝大多数办公自动化任务。  
**设计原则**：零外部依赖（仅依赖 NewLife.Core），直接操作 Open XML（ZIP + XML）/ PDF / OLE2 文件结构，兼顾功能全面与高性能。  
**说明**：CSV 读写操作已由 NewLife.Core 中的 `CsvFile` 类提供支持，本项目不重复实现。

---

## 2. Excel 功能需求

### 2.1 文件读写

- 读取和写入 xlsx 格式（Open XML，直接操作 ZIP + XML）
- 读取和写入 xls 格式（OLE2/BIFF8，兼容 Excel 97-2003）
- 多工作表支持（按名称/索引访问）
- 共享字符串处理
- 流式读写（低内存消耗，适合大文件）

### 2.2 单元格与样式

- 单元格类型自动识别（文本、数值、日期、布尔、百分比）
- 长数字保留文本（防科学计数法）、身份证号/前导零保护
- 字体（名称/大小/粗体/斜体/下划线/颜色）、背景填充色、边框
- 对齐方式（水平/垂直/自动换行）
- 自定义数字格式（`#,##0.00`、`yyyy-MM-dd` 等）
- 条件格式（数值条件高亮、数据条、色阶）
- 富文本（单元格内多种格式混排）

### 2.3 布局与布局操作

- 列宽（自动估算、手动设置）、行高（手动/自动）
- 合并单元格（读取/写入）
- 冻结窗格（行、列、行+列）
- 自动筛选
- 隐藏行/列、分组折叠

### 2.4 内容元素与高级能力

- 公式（读写，不含计算引擎）
- 超链接（URL、邮箱、工作表内部跳转）
- 数据验证（下拉列表、数值范围、日期范围）
- 图片插入与读取（drawing/media）- **图表**（柱状/折线/饼图/面积/散点图，创建与读取）
- **数据透视表**（基本创建与读取）- 对象映射导入/导出（`IEnumerable<T>` ↔ Excel，支持 `[DisplayName]`/`[Description]`）
- 模板填充（基于 xlsx 模板的占位符替换）
- 导出为 CSV（通过 NewLife.Core CsvFile 支持）

---

## 3. Word 功能需求

### 3.1 基础读写

- 读取/写入 docx（解析 ZIP + XML）
- 读取 doc 格式（OLE2 容器，兼容 Word 97-2003）
- 段落与样式（Heading、段落格式、粗体/斜体/颜色）
- 表格读取/写入
- 图片插入/提取
- 目录（TOC）、页眉页脚、分页符支持

### 3.2 模板填充与占位符

- 基于 docx 模板替换 `{{Key}}` 占位符，保留原有样式
- 表格区域填充（模板中标记数据区域，自动扩展行）
- 图片占位符替换

### 3.3 页面与排版

- 页面大小、方向、页边距设置
- 段落间距、行距、缩进、对齐
- 文档属性（标题/作者/主题）

### 3.4 高级功能（中低优先级）

- 书签、批注、脚注/尾注（按需实现）
- 文档保护（只读/密码）
- 对象映射导出（`IEnumerable<T>` → Word 表格）

### 3.5 格式转换与导出

- **转为 PDF（内容翻译型，纯 C# 实现）**：将 docx 段落、表格映射为 PDF 基本元素，不保证像素级一致性。“`WordPdfConverter`已实现”
  > ⚠️ 允许精度限制：当前实现为内容映射型（段落文本 + 表格），不支持完整排版保真。高保真转换可考虑 LibreOffice headless 集成（平台依赖）或 Aspose.Words（商业许可）。
- **转为 HTML**：将 docx 结构映射为语义化 HTML（段落→`<p>`，表格→`<table>`，内联样式→CSS）。“`WordHtmlConverter`已实现”
- **转为图片**（每页渲染为 PNG/JPEG）：需要渲染引擎支持，建议通过 PDF 中转（docx→pdf→image）实现。

---

## 4. PPT 功能需求

### 4.1 文件读写

- 读取和创建 pptx（Open XML）
- 读取 ppt 格式（OLE2 容器，兼容 PPT 97-2003）
- 提取幻灯片文本、形状、表格、备注内容

### 4.2 幻灯片内容

- 文本框（字体/大小/颜色/粗体/斜体/对齐）
- 基本形状（矩形/圆/椭圆/箭头/线条）
- 插入图片（PNG/JPEG，指定位置与尺寸）
- 表格操作（创建/读取，支持样式）
- 超链接（文本/形状跳转）、备注页
- 图表（柱状/折线/饼图，基本生成与读取能力）

### 4.3 布局与设计

- 幻灯片母版与版式、主题/配色（基础支持）
- 背景设置（纯色/渐变/图片）
- 幻灯片大小（4:3 / 16:9 / 自定义）

### 4.4 幻灯片操作

- 添加/复制/移动/删除幻灯片
- 合并演示文稿
- 幻灯片切换（基础过渡/动画优先级低）

### 4.5 模板填充

- 占位符替换（保留原样式）、表格区域填充、图片占位符替换
- 对象映射导出（`IEnumerable<T>` → PPT 表格）

### 4.6 格式转换与导出

- **转为 PDF**：将每张幻灯片转换为 PDF 页面（可基于 PdfSharp 实现基础版）。
- **转为图片**（每张幻灯片渲染为 PNG/JPEG）：需要渲染能力，建议通过 PDF 中转实现（pptx→pdf→image）。

### 4.7 ShapeCrawler 等效形状操作

- **形状文本精确读写**：读写局部 run 级别文本格式（字体/颜色/大小/粗斜体），保留 Open XML 下层 `<a:rPr>` 结构。
- **表格单元格格式**：单元格背景色、边框、文本格式独立控制。
- **图表数据读写**：读取并修改图表系列数据（内嵌 Excel）。
- **跨文件幻灯片复制**：将幻灯片从一个 pptx 复制到另一个（含源文件样式关联）。
- **形状局位精确控制**：获取/设置形状精确位置（EMU/点）和大小。

---

## 5. PDF 功能需求

### 5.1 创建与生成

- 从零创建 PDF 文件（不依赖完整浏览器引擎）
- 文本排版（字体/大小/颜色/粗体/斜体）、中文字体支持
- 多页文档、表格、图片插入、页眉页脚

### 5.2 读取与提取

- 文本提取（逐页/全文）、元数据读取、页面信息
- 结构化数据提取（表格）优先级中等

### 5.3 编辑与操作

- 合并/拆分 PDF、页面旋转/删除、叠加文字/图片（盖章/水印/页码）
- 表单填充（AcroForm）及书签/大纲支持（中优先级）

### 5.4 高级功能（低优先级）

- 加密/权限控制、数字签名（需要额外依赖或商业组件）、PDF/A 合规（可选）

### 5.5 Fluent PDF 生成（QuestPDF 风格）

- **声明式布局 API**：支持 Component 嵌套组合（Row / Column / Text / Image / Table），可链式调用。
- **自动分页**：内容超出单页高度时自动扩展到新页，无需手动计算页数。
- **组件复用**：可定义可复用的布局模板块，在多个文档/页面共享。
- **组合输出**：同一布局模型根据需要输出 PDF 或 HTML（备选）。

### 5.6 底层操作（PdfSharp / MigraDoc 风格）

- **PDF 合并/拆分**：支持多文件合并、按页拆分导出。
- **低层绘图 API**：画线、矩形、弧形等基础图元，适合自定义图表/设计稿场景。
- **页面渲染为图片**：将 PDF 每页渲染为 PNG/JPEG（需引入渲染依赖，如 Docnet/SkiaSharp）。

---

## 6. 不做的功能

以下功能由于实现复杂度高、体积或收益有限，当前不纳入计划：

### Excel

| 功能 | 原因 |
|------|------|
| 公式计算引擎 | 实现复杂，公式读写已支持，无内置计算引擎 |
| VBA/宏 | 安全与复杂度问题 |

### Word

| 功能 | 原因 |
|------|------|
| 修订追踪 | 实现复杂、场景有限 |

### PDF

| 功能 | 原因 |
|------|------|
| HTML→PDF 高保真转换 | 需要完整渲染引擎/浏览器，体积不可控 |
| OCR 文字识别 | 需引入专业 OCR 服务，与本库定位不符 |

### PPT

| 功能 | 原因 |
|------|------|
| 完整动画时间轴 | 规范复杂，优先级低 |

---

## 7. OLE2/BIFF8 跨格式基础层

### 7.1 OLE2/CFB 容器（Compound File Binary）

OLE2（Compound File Binary，CFB）是 Microsoft Office 97-2003 时代所有二进制文档格式（.xls、.doc、.ppt）共用的容器格式。需从零实现 CFB 解析/写入器，作为 xls/doc/ppt 三种格式的共用基础层。

#### 7.1.1 CFB 读取需求

- 解析复合文档头部（512 字节 Header）：版本、扇区大小、FAT/miniFAT 位置
- FAT（文件分配表）遍历：将散布磁盘的扇区链接为流（Stream）
- mini-FAT 处理：< 4096 字节的小流使用 mini-Stream
- 目录项（Directory Entry）遍历：按红黑树结构枚举所有 Storage / Stream
- 按名称访问流（如 "Workbook"、"WordDocument"、"\1CompObj"）
- 多版本兼容（v3 = 512B 扇区, v4 = 4096B 扇区）

#### 7.1.2 CFB 写入需求

- 从零生成最小合法 CFB 文件
- 写入一个或多个 Stream
- 生成正确的 FAT/mini-FAT 和目录树

### 7.2 Excel xls/BIFF8 格式（M17）

详见第 2 章 M17 模块——在 CFB 容器中读写 BIFF8 格式工作簿流（Workbook stream）。

#### 7.2.1 BIFF8 读取

- 解析 BOF/EOF 记录标识工作表边界
- SST（Shared String Table）记录读取
- DIMENSIONS / ROW / LABELSST / NUMBER / FORMULA / BLANK 核心记录
- XF / FONT / FORMAT 样式记录，还原单元格字体/颜色/数字格式
- NAME 记录（定义名称）、MERGEDCELLS（合并区域）、HYPERLINK（超链接）

#### 7.2.2 BIFF8 写入

- 生成 BOF / SST / DIMENSIONS / ROW / 单元格记录序列
- 支持字符串/数值/日期/布尔类型写入
- 支持基本样式（字体/边框/颜色）

### 7.3 Word doc/MS-DOC 格式（W07）

- 从 CFB 容器中提取 `WordDocument` 和 `1Table` / `0Table` 流
- 解析 FIB（File Information Block）获取文档结构偏移量
- 遍历文本字符（cp）范围，提取段落纯文本
- 识别基本段落格式（标题级别、粗体/斜体）
- 写入支持（低优先级）：生成最小合法 .doc 文件

### 7.4 PPT ppt/MS-PPT 格式（S08）

- 从 CFB 容器中提取 `PowerPoint Document` 流  
- 遍历「User Edit Atom」→「Persist Directory」定位最新版本
- 解析 `SlideAtom` + `TextBytesAtom`/`TextCharsAtom` 提取幻灯片文本
- 写入支持（低优先级）

---

## 8. Markdown 文档

### 8.1 读取

- 解析 CommonMark 标准 Markdown 语法：标题/段落/代码块/列表/引用/粗斜体/超链接/图片
- 提取结构化文档树（AST），供进一步转换使用
- 提取内嵌图片引用路径/URL

### 8.2 写入

- 将结构化内容（标题/段落/列表/表格/代码/图片）序列化为 Markdown 文本
- 支持 GFM（GitHub Flavored Markdown）扩展：任务列表、删除线、表格

### 8.3 格式转换

- **Markdown → HTML**：输出语义化 HTML（标题→`<h1>`-`<h6>`，列表→`<ul>/<ol>`，表格→`<table>`）
- **Markdown → Word（docx）**：将 Markdown 标题/段落/列表映射为 Word 样式
- **Markdown → PDF**：通过中转（Markdown→HTML→PDF 或 Markdown→Word→PDF）

---

## 9. RTF 文档

### 9.1 读取

- 解析 RTF 控制字和控制词，提取段落文本、字体、颜色、段落格式
- 提取嵌入图片（WMF / EMF / PNG / JPEG）
- 提取表格（行/列/单元格文本）
- 读取文档属性（标题/作者/修订信息）

### 9.2 写入

- 生成合法 RTF 文件结构（RTF 头部 + 字体表 + 颜色表 + 内容）
- 支持段落格式（字体/大小/粗体/斜体/颜色/对齐/缩进/间距）
- 支持表格写入（基础行列结构）
- 支持图片嵌入（PNG/JPEG，十六进制编码）
- 文档属性写入

### 9.3 模板填充

- 基于 RTF 模板替换 `{{Key}}` 占位符，保留原有段落格式

---

## 10. ODS（OpenDocument Spreadsheet）

ODS 是 LibreOffice / OpenOffice 的原生电子表格格式（ZIP + OpenDocument XML）。

### 10.1 读取

- 解析 `content.xml` 中的工作表数据（行/列/单元格）
- 单元格类型识别（string/float/date/boolean/formula）
- 读取合并单元格（`table:number-columns-spanned` / `table:number-rows-spanned`）
- 读取基础样式信息（字体/颜色/对齐）

### 10.2 写入

- 生成合法 .ods 文件（`mimetype` + `content.xml` + `styles.xml` + ZIP 结构）
- 多工作表写入
- 基础单元格样式（字体/背景/边框/对齐）
- 公式写入（OpenFormula 格式）

### 10.3 与 Excel 互转

- `.ods` → Excel 对象模型（`IEnumerable<Object[]>`）/ DataTable
- `.xlsx` → `.ods` 转换（限基础数据类型和样式）

---

## 11. 邮件存档（Email）

### 11.1 EML 格式（RFC 5322 + MIME）

- 解析 MIME 头部（From/To/Cc/Bcc/Subject/Date/Message-ID/Reply-To）
- 多部分解析（multipart/mixed/alternative/related）
- 正文提取（text/plain 和 text/html）
- 附件提取（文件名、内容类型、二进制数据）
- 内嵌图片（Content-ID 引用）
- Q 编码 / Base64 解码（RFC 2047）
- 创建/发送 .eml（生成合法 MIME 文档）

### 11.2 MSG 格式（Outlook Message，OLE2 容器）

- 从 OLE2 容器提取 MAPI 属性（PR_SUBJECT / PR_BODY / PR_HTML_BODY / 收件人列表）
- 提取附件（MAPI Attachment 对象）
- 读取发件人/收件人元数据
- 写入支持（低优先级）

---

## 12. iCalendar（.ics）

### 12.1 读取

- 解析 iCalendar 3.0/4.0 格式（RFC 5545）
- 提取 VEVENT 组件（标题/描述/开始/结束时间/位置/重复规则/提醒）
- 提取 VTODO / VJOURNAL / VFREEBUSY 组件
- 时区处理（VTIMEZONE 组件，UTC 偏移）
- 重复规则（RRULE）解析（每日/每周/每月/每年）

### 12.2 写入

- 生成符合 RFC 5545 的 .ics 文件
- 创建 VEVENT（日程事件）、VTODO（待办事项）
- 支持重复规则（RRULE）写入
- 多日历条目导出

---

## 13. vCard 联系人名片（.vcf）

### 13.1 读取

- 解析 vCard 3.0/4.0 格式（RFC 6350）
- 提取核心属性：FN/N（姓名）、ORG/TITLE（组织职位）、TEL（电话）、EMAIL、ADR（地址）、PHOTO（头像）、URL、NOTE、BDAY（生日）
- 分组（GROUP）属性解析
- Base64 编码的 PHOTO 提取为字节数据

### 13.2 写入

- 生成符合 RFC 6350 的 .vcf 文件（vCard 4.0 优先，可降级到 3.0）
- 写入所有核心属性
- PHOTO 支持嵌入式 Base64 编码和 URL 引用两种模式

---

## 14. EPUB 电子书

### 14.1 读取

- 解析 EPUB 2/3 结构（ZIP + OPF/NCX/Nav Document）
- 读取书籍元数据（标题/作者/出版商/ISBN/语言/封面）
- 提取目录结构（NCX/Nav，章节层次）
- 提取各章节 HTML 内容（xhtml 文件）
- 提取嵌入图片、CSS 样式表、字体

### 14.2 写入

- 生成合法 EPUB 3 文件（`mimetype` + `META-INF/container.xml` + OPF + XHTML + ZIP）
- 写入元数据和目录
- 添加章节（XHTML 内容）
- 嵌入封面图片和基础 CSS

---

## 15. XPS 固定版式文档（.xps / .oxps）

### 15.1 读取

- 解析 XPS 文件结构（ZIP + `/Documents/1/FixedDocumentSequence.fdseq`）
- 提取各页面 XAML（FixedPage）
- 文本内容提取（`Glyphs` 元素 → 文本字符串）
- 图片提取（嵌入 PNG/JPEG 资源）
- 提取页面尺寸、页数

### 15.2 写入

- 生成最小合法 XPS/OXPS 文件
- 写入文本（单一字体，无复杂布局）
- 插入图片
- 写入元数据（标题/作者）

---

## 16. 目标用户场景

1. 数据报表导出（管理后台导出用户列表、订单明细、统计报表）
2. 数据导入（批量导入用户、产品、配置等）
3. 模板报告（月报、对账单、发票等）
4. 合同/协议生成（基于 Word 模板自动填充）
5. PDF 报告生成与合并拆分
6. 演示文稿生成与模板填充（PPT）
7. 文档内容提取（PDF/PPT/Word）
8. 系统间文件交换（Excel/Word/PPT/PDF）
9. 历史文档迁移（读取旧版 .xls/.doc/.ppt 并转换为现代格式）
10. 邮件系统归档处理（解析 .eml/.msg，提取附件/正文）
11. 日历/会议数据互通（.ics 文件生成与解析）
12. 联系人名片导入导出（.vcf 与 CRM 系统对接）
13. 技术文档生成（Markdown 源码 → HTML/Word/PDF）
14. 电子书内容提取与生成（EPUB）
15. 政务/金融领域 XPS 文档处理

---

（完）
